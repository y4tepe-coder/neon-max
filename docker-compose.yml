version: '3.8'

services:
  neon-agentur:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:3000"
    env_file:
      - .env.runtime
    environment:
      - NODE_ENV=production
    restart: unless-stopped
